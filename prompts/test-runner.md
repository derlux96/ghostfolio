Make a small improvement to the project:

1. Add a GHOSTFOLIO_VERSION env variable to the api .env.example file with value "2.250.0-custom"
2. Add a comment "# Custom fork for Lux - German investor portfolio" to the top of the .env.example file

These are tiny changes, just to verify the runner script works. Commit with: git commit -m "chore: add custom fork version marker"
